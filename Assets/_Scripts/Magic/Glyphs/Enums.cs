public enum GlyphType
{
    Form,
    Effect,
    Modifier
}

public enum Forms
{
    Self,
    Projectile,
    MagicSphere
}

public enum Effects
{
    Launch,
    Enlarge,
    Reduce,
}

public enum Modifiers
{
    Amplify,
    Accelerate,
    Decelerate
}