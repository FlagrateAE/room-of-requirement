public enum GlyphType
{
    Form,
    Effect,
    Modifier
}

public enum Form
{
    Self,
    Projectile,
    MagicSphere
}

public enum Effect
{
    Launch,
    Enlarge,
    Reduce,
}

public enum Modifier
{
    Amplify,
    Accelerate,
    Decelerate
}