public enum GlyphType
{
    Form,
    Effect,
    Modifier
}

public enum Form
{
    Self,
    Projectile,
    MagicSphere
}

public enum Effect
{
    Launch,
    Enlarge
}

public enum Modifier
{
    Amplify,
    Accelerate,
    Decelerate
}